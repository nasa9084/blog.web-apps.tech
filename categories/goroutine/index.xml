<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom" xmlns:content="http://purl.org/rss/1.0/modules/content/"><channel><title>goroutine on something tech.</title><link>https://blog.web-apps.tech/categories/goroutine/</link><description>Recent content in goroutine on something tech.</description><generator>Hugo -- gohugo.io</generator><language>ja-jp</language><copyright>&amp;copy; @nasa9084</copyright><lastBuildDate>Wed, 21 Nov 2018 08:09:23 +0000</lastBuildDate><atom:link href="https://blog.web-apps.tech/categories/goroutine/index.xml" rel="self" type="application/rss+xml"/><item><title>Generator Pattern</title><link>https://blog.web-apps.tech/generator-pattern/</link><pubDate>Wed, 21 Nov 2018 08:09:23 +0000</pubDate><guid>https://blog.web-apps.tech/generator-pattern/</guid><description>Generator PatternはGo言語における並列処理パターンの一つで、goroutine-safeな値列の生成などに使用することができます。
コードを見た方が早いと思いますので、コードを掲載しましょう。 次の例は複数のgoroutineから共通の連番を採番したいときに利用することができます。
func GenInt(ctx context.Context, max int) &amp;lt;-chan int { ch := make(chan int) go func() { defer close(ch) for i := 0; i &amp;lt; max; i++ { select { case &amp;lt;-ctx.Done(): return case ch &amp;lt;- i: } } return ch } 返された&amp;lt;-chan intからintの値を取得するようにすることで、重複のない連番を取得することができます。 Go言語において、chanは複数箇所から値の取り出しを行うことができますが、chanに入力された一つの値はどこか一箇所からしか取り出すことができません。そのため、lock等を使用しなくとも、必ず重複無く連番を取得することができます。 lockを使用した場合、若干動作が遅いため、可能であればlockを使用しないで、chanを使用して実装できるとより高速な、Goらしいコードとすることができます。</description></item><item><title>Future Pattern</title><link>https://blog.web-apps.tech/future-pattern/</link><pubDate>Wed, 04 Jul 2018 15:33:02 +0000</pubDate><guid>https://blog.web-apps.tech/future-pattern/</guid><description>Future Patternは非同期処理パターンの一つで、ある処理を別のスレッドなどで実行し、結果を後で(=未来で)受け取るような処理に用いられるデザインパターンです。 特徴としては、外側に見えている関数などの処理を実行するオブジェクトは、処理を別スレッドに委譲し、後で結果を得ることの出来るFutureと呼ばれるオブジェクトを即座にメインロジックへと返却することです。
言葉で書いても、何だかよくわからないので、コードを見てみましょう。
/* package, import part */ func main() { in := make(chan int) out := Double(in) // この時点では結果は得られない go func() { for i := 0; i &amp;lt; 10; i++ { in &amp;lt;- i } close(in) }() for d := range out { fmt.Println(d) // ここで結果を得る } } func Double(in &amp;lt;-chan int) &amp;lt;-chan int { out := make(chan int) go func() { for i := range in { out &amp;lt;- 2 * i } close(out) } return out // Futureオブジェクト } main関数から呼び出されたDouble関数は、与えられた数を二倍する関数ですが、二倍する処理は呼び出された時点では実行せず、即座にchannelを返します。この、変数名outのchannelがFutureオブジェクトです。 そのため、数を二倍した結果は、Double関数を呼び出した時点では得られず、後でoutchannelから得ることとなります。</description></item><item><title>Golang: 手軽にシグナルをListenしてcallback関数を呼ぶ</title><link>https://blog.web-apps.tech/go-signal-more-simply/</link><pubDate>Tue, 06 Mar 2018 14:32:00 +0000</pubDate><guid>https://blog.web-apps.tech/go-signal-more-simply/</guid><description>Go言語でシグナルを取り扱いたい場合、osパッケージおよびos/signalパッケージ、syscallパッケージを使用します。
具体的には、以下のようにします。
func main() { sigCh := make(chan os.Signal, 1) doneCh := make(chan struct{}) signal.Notify(sigCh, syscall.SIGINT) go func() { sig := &amp;lt;-sigCh fmt.Println(sig) // (1) close(doneCh) }() &amp;lt;-done } 実際には、(1)の様に受け取ったシグナルを出力するだけではなく、何らかの処理を行うことになるでしょうし、goroutineのリークを避けるためにシグナルの待受をキャンセルする必要が有りますから、contextを使用してfor-selectループを書くことにもなるでしょう。
例として、HTTPサーバをシャットダウンするような処理を考えます。
func main() { sigCh := make(chan os.Signal, 1) doneCh := make(chan struct{}) ctx, cancel := context.WithCancel(context.Background()) signal.Notify(sigCh, syscall.SIGINT) s := &amp;amp;http.Server{ Addr: &amp;#34;:8080&amp;#34;, Handler: http.</description></item><item><title>sync.WaitGroup</title><link>https://blog.web-apps.tech/how-to-use-sync-waitgroup/</link><pubDate>Fri, 01 Sep 2017 07:03:07 +0000</pubDate><guid>https://blog.web-apps.tech/how-to-use-sync-waitgroup/</guid><description>Goroutineを使用して複数の処理を並列で実行、すべてが終わったら次の処理に進みたいという場合があると思います。 Goroutineでデータのリストを作るという処理を考えます。 データの順番は関係なく、すべてのGoroutineでのデータがそろったら次の処理をしたいという設定です。 この場合、単純に考えると以下のようなコードになりますが、以下のコードではデータがそろう前に次の処理が行われます。
datalist := []string{} for i := 0; i &amp;lt; 10; i++ { go func() { // something w/datalist } } fmt.Println(&amp;#34;next step&amp;#34;) このような場合に、sync.WaitGroupを使用します。 sync.WaitGroupは基本的にはただのカウンタですが、カウンタがゼロになるまで処理を待つことができます。 言葉で説明してもわかりにくいと思いますので、ソースコードを見てみましょう。
datalist := []string{} wg := sync.WaitGroup{} for i := 0; i &amp;lt; 10; i++ { wg.Add(1) // Goroutineの数だけカウンタを増やす go func() { // something w/datalist wg.Done() // カウンタを減らす } } wg.Wait() // カウンタが0になるまでブロックする fmt.</description></item></channel></rss>